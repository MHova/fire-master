# FIREMaster settings — this file is filled in automatically on first run by:
#   docker compose run --rm backend uv run python -m app.setup
#
# Leave this file here (it must exist before the first run). Do not delete it.
# After setup, it will contain your admin login and app secrets.
